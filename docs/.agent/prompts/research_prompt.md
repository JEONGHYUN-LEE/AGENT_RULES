Read `docs/.agent/rules/agent_constitution.md` and `docs/.agent/rules/research_rule.md`.

First perform internal analysis of the repository. Do not write under `docs/knowledge/` yet.

Follow this research order:
1. repository topology (and codebase boundaries if multiple)
2. entry points
3. runtime execution flows
4. major module responsibilities
5. important details and unknowns

After the analysis is complete, create or update .md files under `docs/knowledge/`. Use as many files and subfolders as needed; split or reorganize for clarity and scale. Maintain an index or README in `docs/knowledge/` so readers can find and use all .md files there.

Do not create plans. Do not modify code.
